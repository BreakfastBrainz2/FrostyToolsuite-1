<ctrl:FrostyDockableWindow x:Class="FNVHashCalculatorPlugin.Windows.FNVHashCalculatorWindow"
                           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                           xmlns:local="clr-namespace:FNVHashCalculatorPlugin.Windows"
                           xmlns:ctrl="clr-namespace:Frosty.Controls;assembly=FrostyControls"
                           mc:Ignorable="d"
                           Title="FNV-1 Hash Calculator"
                           Height="170"
                           Width="650"
                           ResizeMode="NoResize"
                           WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FrostyControls;component/Themes/Generic.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Background="{StaticResource WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="38" />
        </Grid.RowDefinitions>

        <Grid Background="{StaticResource ListBackground}">
            <StackPanel>
                <DockPanel LastChildFill="True"
                           Margin="5">
                    <Label Content="GUID: "
                           FontFamily="Global User Interface"
                           Width="125" />
                    <TextBox x:Name="varGUIDNameTextBox"
                             VerticalContentAlignment="Center"
                             Text=""
                             Margin="1"
                             BorderThickness="1"
                             MaxLength="36"
                             TextChanged="GUIDNameTextBox_TextChanged" />
                </DockPanel>
                <DockPanel LastChildFill="True"
                           Margin="5">
                    <Label Content="Output: "
                           FontFamily="Global User Interface"
                           Width="125" />
                    <TextBox x:Name="varHashOutputTextBox"
                             VerticalContentAlignment="Center"
                             Text=""
                             Margin="1"
                             BorderThickness="1" 
                             IsReadOnly="True" />
                </DockPanel>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1">
            <Grid Margin="8">
                <DockPanel LastChildFill="False">
                    <Button x:Name="CancelButton"
                            Content="Cancel"
                            DockPanel.Dock="Left"
                            Width="75"
                            Click="CancelButton_Click" />
                    <Button x:Name="CalculateHashButton"
                            Content="Calculate Hash"
                            DockPanel.Dock="Right"
                            Width="95"
                            Click="CalculateHashButton_Click" />
                </DockPanel>
            </Grid>
        </Border>
    </Grid>
</ctrl:FrostyDockableWindow>
