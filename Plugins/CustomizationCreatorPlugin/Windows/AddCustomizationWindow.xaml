<ctrl:FrostyDockableWindow x:Class="CustomizationCreatorPlugin.Windows.AddCustomizationWindow"
                           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                           xmlns:local="clr-namespace:CustomizationCreatorPlugin.Windows"
                           xmlns:ctrl="clr-namespace:Frosty.Controls;assembly=FrostyControls"
                           mc:Ignorable="d"
                           Title="Create Customization"
                           Height="170"
                           Width="650"
                           ResizeMode="NoResize"
                           WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FrostyControls;component/Themes/Generic.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Background="{StaticResource WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="38" />
        </Grid.RowDefinitions>

        <Grid Background="{StaticResource ListBackground}">
            <StackPanel>
                <DockPanel LastChildFill="True"
                           Margin="5">
                    <Label Content="Name: "
                           FontFamily="Global User Interface"
                           Width="125" />
                    <TextBox x:Name="varWepNameTextBox"
                             VerticalContentAlignment="Center"
                             Text=""
                             Margin="1"
                             BorderThickness="1"
                             TextChanged="varWepNameTextBox_TextChanged" />
                </DockPanel>
                <DockPanel LastChildFill="True"
                           Margin="5">
                    <Label Content="Blueprint Directory: "
                           FontFamily="Global User Interface"
                           Width="125" />
                    <TextBox x:Name="varBPDirTextBox"
                             VerticalContentAlignment="Center"
                             Text=""
                             Margin="1"
                             BorderThickness="1"
                             TextChanged="varBPDirTextBox_TextChanged" />
                </DockPanel>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1">
            <Grid Margin="8">
                <DockPanel LastChildFill="False">
                    <Button x:Name="cancelButton"
                            Content="Cancel"
                            DockPanel.Dock="Left"
                            Width="75"
                            Click="cancelButton_Click" />
                    <Button x:Name="createButton"
                            Content="Create Weapon"
                            DockPanel.Dock="Right"
                            Width="95"
                            Click="createButton_Click" />
                </DockPanel>
            </Grid>
        </Border>
    </Grid>
</ctrl:FrostyDockableWindow>
